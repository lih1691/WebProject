import styled from 'styled-components';
import oc from 'open-color';

export const SectionTitle = styled.div`
  position: relative;
  padding-top: 200px;
  margin-bottom: 50px;
  color: ${oc.gray[9]};
`